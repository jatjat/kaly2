package ca.joelathiessen.util.image
/*
import ca.joelathiessen.util.image.android.AndroidImage
import ca.joelathiessen.util.image.jvm.JVMImage
import ca.joelathiessen.util.isAndroid

class AndroidJVMImageFactory() {
    fun makeImage(filePath: String): AndroidJVMImage {
        if(isAndroid()) {
            return AndroidImage(filePath)
        } else {
            return JVMImage(filePath)
        }
    }
}*/